package com.portfoliotrack.config;

/**
 * STUB — intentionally not a Spring @Component.
 * The active JWT filter is: com.portfoliotrack.security.JwtAuthFilter
 * which is wired into SecurityConfig.
 */
public class JwtAuthFilter {
    // Not used. See com.portfoliotrack.security.JwtAuthFilter.
}
