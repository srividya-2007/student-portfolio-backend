package com.portfoliotrack.controller;

/**
 * STUB — intentionally not a @RestController.
 * All file-upload endpoints are handled by UploadController
 * which maps to /api/upload/**.
 *
 * Having two controllers on the same request mapping causes an
 * "Ambiguous mapping" startup failure.
 */
public class FileUploadController {
    // Not used. See UploadController.java.
}
